/**
 * Bootstrap JS chạy trên mọi trang. Không nhồi toàn bộ logic vào đây —
 * mỗi hành vi cụ thể nằm ở assets/src/js/layout hoặc assets/src/js/pages.
 */
document.documentElement.classList.remove( 'no-js' );
document.documentElement.classList.add( 'js' );
